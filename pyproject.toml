[project]
name = "slackdown"
version = "0.1.0"
description = "A tool for slurping Slack history to a markdown file"
readme = "README.md"
requires-python = ">=3.13.0"
dependencies = [
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
]
